// Phase 0 stub (BUILD-PLAN: "Deliberately absent: LLM calls").
// MC-104 lands complete() here; this remains the ONLY package allowed to
// import provider SDKs (CLAUDE.md invariant 3).
export const LLM_PACKAGE = "@mission-control/llm";
