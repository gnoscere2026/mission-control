// Worker entrypoint — replaced with the real cadence engine in MC-003 (Task 5).
import { CORE_PACKAGE } from "@mission-control/core";

console.log(`worker skeleton (${CORE_PACKAGE}) — cadence engine lands in MC-003`);
