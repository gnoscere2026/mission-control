# Mission Control — Risk Register (v1)

**Deliverable §9.6** · review at the end of every phase; promote anything that fires into an INSIGHTS entry + ticket.

Likelihood/Impact: L/M/H. "Trigger" = the observable signal that the risk is materializing, so it's checked, not vibes.

| # | Risk | L | I | Trigger / indicator | Mitigation (built) | Contingency (if it fires) |
|---|---|---|---|---|---|---|
| R1 | **iOS PWA push unreliability.** iOS web push requires home-screen install; iOS has historically dropped subscriptions after OS updates or app eviction. A missed Morning Brief breaks the habit loop (brief tolerates zero). | H | M | `push_subscriptions.failure_count` rising; `pushed_at` null while `emailed_at` set; gate metric 1 dipping while email opens stay flat | Email mirror on **every** brief (locked, §2.1); per-channel delivery status (MC-204); settings surface "push unhealthy since…" | If push <80% reliable after Phase 2: treat email as primary and push as bonus; consider Telegram-bot delivery as a v1.x add-on (still Level-2: delivery only) |
| R2 | **Google OAuth app verification / token expiry.** `gmail.readonly` is a *restricted* scope. A GCP OAuth app in **Testing** status expires refresh tokens every 7 days → silent ingest death weekly. Full verification (security assessment) is unrealistic for a personal app. | H | H | `ingest_gmail` failing with `invalid_grant` weekly; `google_accounts.gmail_last_sync_at` stale | **Decide in MC-101:** if Mark has/creates a Google Workspace account, mark the OAuth app **Internal** — no verification, no 7-day expiry (recommended; ~$7/mo Workspace seat is the cheap fix). | If stuck on consumer gmail.com: keep app in Testing and build a one-tap re-consent flow + push alert when `invalid_grant` appears; revisit verification only at design-partner phase |
| R3 | **Extraction precision too low to trust.** If the confirmation queue is mostly noise, Mark stops processing it and the ledger dies (kills graduation gates 2 and 3). | M | H | Eval precision plateau; rejection rate >50% in `extraction_labels` over a week; queue items aging unprocessed | Eval harness from week one (MC-106); rejections are labeled signal; human-in-loop keeps noise out of the *ledger* even when the *queue* is noisy; prompt versions gated on eval runs | Tighten the prompt to high-precision/lower-recall (candidates only above confidence floor); add a "low-confidence" collapsed queue section; escalate extraction tier cheap→mid (config change + eval run) |
| R4 | **BullMQ durability limits.** Repeatable jobs lost on Redis data loss; multi-step jobs (ingest→extract chains) lack exactly-once across process death; delayed T−45 jobs vulnerable to drift after outages. | L | M | **Graduation triggers (locked in brief §2.3):** (a) any silently-lost scheduled run discovered after the fact, twice; (b) a workflow needing >2 chained jobs with transactional hand-off; (c) human-in-the-loop waits measured in days inside a job | Deterministic jobIds + DB-derived state (scheduler scan job *recomputes* desired delayed jobs from `calendar_events`, so Redis is a cache of intent, not the source of truth); `cadence_runs` makes gaps visible | Port handlers to Temporal — seam documented in ARCHITECTURE §5.3; handlers are plain functions, BullMQ confined to `src/queues/` |
| R5 | **Model cost creep.** Per-day cost drifts up with prompt/context growth; an Opus-heavy bug (retry loop) could burn real money silently. | M | L | Daily cost ticker (MC-204) above ceiling (initial estimate ≈ $1–2/day: 3–4 Opus artifacts + Haiku extraction + embeddings ≈ $30–60/mo); any single run >$1 | Per-call `model_calls.cost_usd` from day 1; tier routing (Haiku for volume work); bounded retries (1 schema retry, 5 job attempts); ContextPacket token budget (MC-202) | Ceiling alert → push notification; per-run cost cap in `packages/llm` that fails the run visibly past a hard limit; drop brief tier top→mid temporarily (config) |
| R6 | **Gmail quota / sync correctness** (distinct from R2: this is the API mechanics). HistoryId expiry, large backfills, attachments-heavy threads slowing `messages.get`. | L | L | 404-fallback path triggering more than ~monthly; ingest run duration trending up; quota errors in run meta | Quota headroom is ~4 orders of magnitude (ARCHITECTURE §2.3); bounded fallback re-list; metadata-first fetch (bodies excerpted, not full MIME) | Narrow ingest to a label/query (e.g. exclude newsletters via `messages.list` query) — also helps R3 noise |
| R7 | **Personal-data handling mistakes.** Real email content in fixtures, logs, or model-call records; a leak is reputationally fatal for a product whose pitch is trust. | M | H | Any PII spotted in repo/CI logs; fixture review finding un-anonymized content | Anonymization workflow is a *blocking* step in EVAL-SPEC; CLAUDE.md forbids logging raw bodies/tokens; `model_calls` stores data *categories*, never content; sealed-box tokens | Purge + rotate (git history rewrite, key rotation runbook in RUNBOOK.md); add a CI secret/PII scanner (gitleaks) — promote to ticket on first near-miss |
| R8 | **Single-instance data loss.** The ledger is the moat; losing Postgres loses the product's entire accumulated value. | L | H | Backup job absent/failing; restore never tested | Railway scheduled backups + MC-902 restore drill before real data lands (Phase 2 gate) | Weekly `pg_dump` to object storage as second copy (cron in worker) — promote to ticket if Railway backup proves opaque |
| R9 | **Solo-founder cadence risk.** Weekend-session model stalls (life happens); half-built phases rot. | M | M | >3 weeks between sessions; a phase open >6 weeks | Phases shippable in 1–3 sessions *and* each session leaves `main` deployable (BUILD-PLAN exit criteria are per-phase, CI per-session) | Cut scope inside the phase (BUILD-PLAN lists deliberate absences first); never start a new phase to escape an unfinished one |

## Standing review questions (end of each phase)

1. Did any trigger fire? → contingency becomes a ticket now, not a note.
2. Is the daily cost number still boring?
3. Has R2's decision (Workspace internal app vs. testing-mode re-consent) been made and verified for >7 days?
4. Are gate metrics (brief §8) trending toward or away from graduation?
